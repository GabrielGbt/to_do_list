/* import 'package:flutter/material.dart';

import 'package:get/get.dart';

import '../../../ui/homepage/home_page_controller.dart';

class HomePageCard extends GetView<HomePageController> {
  const HomePageCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Card(
        child: Stack(
          children: [
            Text(
              'sdadasd',
              textAlign: TextAlign.left,
            ),
          ],
        ),
      ),
    );
  }
}
 */